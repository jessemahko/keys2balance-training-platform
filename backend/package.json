{
	"name": "backend",
	"version": "1.0.0",
	"main": "index.js",
	"scripts": {
		"test": "jest --verbose --testPathIgnorePatterns=integration",
		"test:integration": "NODE_ENV=test jest --verbose --testPathPatterns=integration --testPathIgnorePatterns=helpers --forceExit",
		"test:all": "NODE_ENV=test jest --verbose --forceExit",
		"dev": "nodemon index.js",
		"start": "node index.js",
		"lint": "eslint .",
		"build:ui": "@powershell Remove-Item -Recurse -Force dist -ErrorAction SilentlyContinue; cd ../frontend; npm run build; Copy-Item dist -Recurse ../backend",
		"build:ui:powershell": "@powershell Remove-Item -Recurse -Force dist -ErrorAction SilentlyContinue; cd ../frontend; npm run build; Copy-Item dist -Recurse ../backend",
		"build:ui:wsl": "bash -c \"rm -rf dist && cd ../frontend && npm run build && cp -r dist ../backend\"",
		"build:ui:mac": "rm -rf dist && cd ../frontend && npm run build && cp -r dist ../backend",
		"build:ui:windows-cmd": "cmd /c \"(if exist dist rmdir /s /q dist) && cd ..\\frontend && npm run build && xcopy dist ..\\backend\\dist /E /I /Y\"",
		"deploy": "fly deploy",
		"deploy:full": "npm run build:ui && npm run deploy",
		"logs:prod": "fly logs"
	},
	"author": "",
	"license": "ISC",
	"description": "",
	"dependencies": {
		"axios": "^1.13.6",
		"bcrypt": "^6.0.0",
		"cors": "^2.8.6",
		"dotenv": "^17.2.4",
		"express": "^4.22.1",
		"express-async-errors": "^3.1.1",
		"jsonwebtoken": "^9.0.3",
		"libphonenumber-js": "^1.12.40",
		"multer": "^2.1.1",
		"nodemailer": "^8.0.1",
		"passport": "^0.7.0",
		"passport-facebook": "^3.0.0",
		"passport-google-oauth20": "^2.0.0",
		"pg": "^8.18.0",
		"validator": "^13.15.26"
	},
	"devDependencies": {
		"@flydotio/dockerfile": "^0.7.10",
		"jest": "^30.3.0",
		"nodemon": "^3.1.11",
		"supertest": "^7.2.2"
	}
}
